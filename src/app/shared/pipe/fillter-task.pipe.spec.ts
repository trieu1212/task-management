import { FillterTaskPipe } from './fillter-task.pipe';

describe('FillterTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FillterTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
